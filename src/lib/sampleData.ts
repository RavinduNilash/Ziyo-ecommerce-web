import { Product, Category } from './types';
import { electronicsCategories, electronicsProducts } from './electronicsData';

// Use electronics data as the main sample data
export const sampleCategories: Category[] = electronicsCategories;
export const sampleProducts: Product[] = electronicsProducts;